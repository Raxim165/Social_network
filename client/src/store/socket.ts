// import { create } from "zustand";

// interface Socket {
//   socket: WebSocket | null;
//   setSocket: (socket: WebSocket | null) => void;
// }

// export const useSocket = create<Socket>()((set) => ({
//   socket: null,
//   setSocket: (socket) => set({ socket }),
// }));